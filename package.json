{
  "name": "ao3-tide-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node scripts/build.js && bunx playwright test",
    "test:ui": "node scripts/build.js && bunx playwright test --ui",
    "report": "bunx playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
